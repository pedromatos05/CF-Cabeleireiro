export default function DashboardPage() {
  return (
    <div>
      <h1 className="mb-4 text-3xl font-bold">Dashboard</h1>
      <p className="text-gray-600">
        Visão geral do dia — marcações de hoje, receita diária, clientes esperados e ocupação por
        profissional.
      </p>
    </div>
  )
}
